interface:
  display_name: "Merge Movies: Update Movie"
  short_description: "Edit existing merge.mov movies, scenes, and narration"
  default_prompt: "Use $update-movie to revise an existing merge.mov movie."

dependencies:
  tools:
    - type: "mcp"
      value: "merge-movies"
      description: "merge.mov MCP server for updating code walkthrough videos"
      transport: "streamable_http"
      url: "https://merge.mov/api/mcp"
