interface:
  display_name: "Merge Movies"
  short_description: "Create and update merge.mov code walkthrough movies"
  default_prompt: "Use $merge-movies to create a new walkthrough movie or update an existing one."

dependencies:
  tools:
    - type: "mcp"
      value: "merge-movies"
      description: "merge.mov MCP server for creating and updating code walkthrough videos"
      transport: "streamable_http"
      url: "https://merge.mov/api/mcp"
